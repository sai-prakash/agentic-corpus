# Context graph

**Definition.** A task-specific runtime projection assembled from governed knowledge, retained experience, live state, and policy. Usually built for one task and discarded.

**Contrast.** A context graph is not a third source of truth and not a product. Knowledge graph holds accepted assertions. Memory graph holds observations and outcomes. Context graph is the slice an agent is allowed to see *now*. Harness is the permission boundary around that slice. GraphMemix operationalizes the slice as a query-aware evidence forest under a max-K budget. Same Model, Different Harness: *how* the slice is rendered under a tight window moves SWE-bench outcomes with frozen weights. Context Gets Root: assembly that promotes a tool block into a higher instruction level is not a slice, it is a privilege change. Graft (trailhq/Graft): the missing slice for coding agents is a repo-local map written once as linked markdown. Same model / Docker / turn limits, Claude Sonnet 5, 50 SWE-bench Verified: 27/50 → 33/50; the failure shape without the map is "patch one file, miss its siblings." IBM: do not promote that map into an event ontology the reader will not traverse. Memoryfield: assemble the slice with a semantic jump over agent-written pages, not a walk of titles. Two tool calls is the budget; an N-hop stroll is a failure mode. GitLab Orbit (beta, docs 6 Sep): a hosted SDLC + source property graph served to *other* harnesses over MCP. Compare the Market, 79 real MRs: Orbit-grounded reviewer ~70% accurate inline comments vs ~58% RAG; RAG lost to no-context. Staples: >2,200 orgs, >170k queries, ~80% of volume from Claude Code / Codex. That is a live context graph consumed as a tool, not a memory product and not a plan fence. ContextPipe (2609.00749): the slice is a planned query. Plan / Bind / Optimize / Execute / Feedback; Bind is the only I/O; Execute is the only model call; volatility sort keeps the cached prefix byte-identical; every applied or skipped transform is an EXPLAIN ANALYZE row. SWE-bench Pro Qutebrowser vs append-only: −31% tokens, −23% LLM calls, −9% duration, lower cache-hit rate because compaction rewrites the prefix. Substrate-aware agents (2609.05232 + manu2/Context-Aware-Agent-Experiment): the slice also has to name the *machine*. A 128 MB / 10 s contract cut peak RSS in 13/14 index-aligned pairs and wall time in all three frontier cohorts; at 96 MB, correct-and-within-budget went 0–1/5 → 3–5/5. A growing string is not a plan. A plan that never names RAM is substrate-blind.

**Heat.** 5. Updated 2026-09-08.

Sources: Graft trailhq/Graft + 2093967894677799122; Same Model, Different Harness 2608.26218; Context Gets Root 2608.27299; Sergey Vasiliev (17 Aug); Year of the Graph 2094370392282464493; GE survey 2608.21156; 0xnoonez three-layer split; GraphMemix 2608.26983; IBM VLDB structure-boundary; calpaterson memoryfield essay + spec; GitLab Orbit blog + gitlabhq/orbit-knowledge-graph + x.com/CEOinterview/status/2096780877628068224; ContextPipe 2609.00749 + x.com/guifav/status/2096527209808679070; Substrate-aware 2609.05232 + manu2/Context-Aware-Agent-Experiment.

Publish angle: one LPG, three write-paths. A label is not a security boundary unless assembly refuses to raise it. A codebase map that lives in git is a context graph, not a product category. A hosted SDLC graph is still a slice an external harness queries. Prefer two-call retrieval over title-walking. Prefer an EXPLAIN trace over an invisible compact. Disclose the resource contract before the first plan, not after the OOM.
