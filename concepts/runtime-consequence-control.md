# Runtime consequence control

**Definition.** A model-free gate that maps each attempted tool or shell effect to a named consequence class and a per-(resource, class) trust state *before* the command runs. Registry scanners and skill-file linters are inputs to that ledger. They are not the decision.

**Contrast.** A clean scan is not a permitted action. After the Party (2609.17274): OpenClaw / ClawHub stock nearly doubled in 91 days; top 10% of skills took 46.93% of downloads; 77.86% have zero stars and zero comments; 85.06% of readable skills carry privilege evidence; three scanners covering 61,990 skills disagreed on 23,702. Scan the Skill (2609.12001 + pheo-ai/open-agent-trust-system): 66,192 public versions; 705 skills across 135 publishers that every scanner *and* the registry judge rate clean still instruct an action prohibited by CIS Control 2.7 / NIST SP 800-53 CM-11 (hand audit n=100, 92% precision, no malice marker; one publisher is 506/705). Of 144 live-agent commands following real skill docs, 34.7% carried a consequence class the document never named. Over 53 cleared skills documenting an action no clean record earns, the agent reached for one in 23; the deterministic gate stopped all 23. NVIDIA SkillSpector is the pre-install scanner those papers assume and then outgrow. MCP silent drift: the registry id can move hosts under you; this gate still has to name the *class* of the call that arrives.

**Heat.** 5. Updated 2026-09-17.

Sources: After the Party 2609.17274; Scan the Skill 2609.12001 + github.com/pheo-ai/open-agent-trust-system; NVIDIA/SkillSpector; Same Name, Different Server 2609.14119.

Publish angle: 6h lab — ship a skill every scanner rates clean that documents `wipe_workspace`. Arm A runs it. Arm B puts a per-(resource, class) ledger on the path with no model in the decide function and emits a receipt on block. Report undocumented classes and whether the clean verdict still executed the prohibited command.
