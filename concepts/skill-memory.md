# Skill memory

**Definition.** Procedural memory as invocable units selected by current working state, not by dumping every skill file into the prompt.

**Contrast.** A skill is not a prompt and not the full trajectory. Recuris: working memory picks from experiential memory. HyperSkill: a trajectory is a hyperedge over subtasks and skills. Graph-of-Skills retrieves a dependency-complete bundle. CaSKG calibrates the *edges* with counterfactual probes before that retrieval. WikiSkill splits the store three ways — raw traces, a persistent wiki of consolidated lessons, executable skills that must build on the wiki. Skills transfer across model families; drop the wiki and evolution stalls. SkillFeed (2608.28241): task-only routing conflates relevance with suitability — hold the task, change the user profile, and the reference skill changes (+35.1 where the profile flips the answer; 75.1% top-1 overall). GCPC (2608.27487): a skill can move the trajectory without moving pass@1; partial credit is the sensor. Warp (Anthropic reprint 26 Aug, Warp docs 1 Sep): two files, not one — an inner skill does the job; an outer skill reads human corrections and opens a PR against the inner `SKILL.md`. Principles beat rule lists; rules overfit the last ticket. The merge is the gate. MNIST-PRO: a skill-shaped persistent write of stitching procedures did not raise accuracy over the episode-local harness. MASkills (2609.02094 + DaRL-GenAI/MASkills): reflection text is not addressable. Credit lands on the skills the trajectory invoked; operators refine / induce / consolidate / prune one file at a time; a held-out gate rolls the library back. HotpotQA F1 76.3 vs MultiPersona 69.2. LoCoMo SH-F1 27.61 vs MemGPT 26.65. GAIA avg 23.3 vs ReAct 18.4; L3 remains 0.0 in their table. TROVE (2609.05019): the skill graph is a *route*. After one top-level skill commits, keep a valid continuation, insert a trace-supported local fix, or replace only the invalid suffix — do not replan the prefix that already ran. Skill-Evo4GUI (2609.04869 + LongtaoHu/Skill-Evo4GUI): computer-use traces compile into a versioned library under a frozen model; four OSWorld domains +5.7 to +18.6 pp after a five-iteration empty-library warm-up. GIMP revisions can fail to recover the originating task. Trace2Tower (2609.05261): the library itself has levels. Canonical events on a transition-and-outcome graph, contrastive spectral modes, then a tower of action templates / routines / strategies. ALFWorld 87.31% at 10.35 steps and 0.26 invalid actions; WebShop exact 50.67%. TROVE edits the remaining route; Trace2Tower compiles what the route may pick. NVIDIA SkillSpector: a `SKILL.md` is also an instruction surface — scan before the next snapshot loads it.

**Heat.** 5. Updated 2026-09-08.

Sources: SkillFeed 2608.28241; GCPC 2608.27487; WikiSkill 2608.27454; Recuris 2608.24876; HyperSkill 2608.16114; Graph-of-Skills 2604.05333; CaSKG 2608.25500; THE DECODER on WikiSkill (29 Aug); Anthropic/Warp self-improving skills; Warp docs self-improving agent; MNIST-PRO 2608.31022; MASkills 2609.02094 + DaRL-GenAI/MASkills; TROVE 2609.05019; Skill-Evo4GUI 2609.04869 + LongtaoHu/Skill-Evo4GUI; Trace2Tower 2609.05261; NVIDIA/SkillSpector.

Publish angle: three files. Traces append-only. Wiki is the only thing a skill may cite. Route on task *and* profile. Outer loop proposes a skill diff; a human or eval merges it. Credit the invoked skill, not the agent. Edit only the suffix the outcome invalidated. Compile levels from transitions, not from the last win paragraph. Scan the file before the next snapshot loads it. Never load the raw trace on the second run.
