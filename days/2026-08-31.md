# 2026-08-31

Same Model, Different Harness (2608.26218, Sydney Lewis): freeze the weights and the task, change only how the harness renders context. Control keeps the full chronological transcript. Treatment shortens older tool results as the window fills and reacts to stalled work, without dropping the record. Tight 20,480-token window, 169 SWE-bench Verified tasks, 480s cap: mean per-task F2PF 28% → 49%, complete solutions 43 → 72. Same frozen treatment lifts Devstral, Nemotron, and Qwen3.8 with no retune. Wide 262k window: Verified and Pro arms converge. The eval unit is the solver (model + harness + context policy), not the checkpoint. Rohan Paul circulating the numbers 31 Aug.

When Context Gets Root (2608.27299): instruction hierarchy is a model-side ranking of sources. The harness rebuilds context every turn and can promote tool output into user- or system-level instructions. Tool-to-user and tool-to-system escalation hit 13 CIA/RCE objectives on six coding-agent harnesses; unrestricted execution succeeds on all six, automatic permission review on all three that offer it. Persistent goals and scheduled tasks reproduce the same hole. Pairs with SARA (induction ≠ authorization) and with Pillar (26 May, recirculated 30 Aug by _MrNiko): the loop, tool registry, compaction, and session log outrank the agent. Compaction that re-roles a tool blob is the exploit, not the shrink.

MemToC (2608.26295): post-tool-return arbitration with known source correctness. 6,504 episodes. Instruction-tuned 7–9B models keep a verified-correct memory against a wrong tool in only 6.5–17.1% of eligible cases, follow a correct tool 86–93%, and parrot the tool when both are wrong 78–86%. Cross-model ranking collapses under three instruction wordings of the same episode. SFT/DPO can raise retain-without-hurting-follow on two of four backbones; 19/20 method–model pairs also cut abstention after tool errors. Memory that cannot refuse a tool is a write-only diary with a loud neighbor.

Predictable harness (2608.26197): first-pass deterministic wrapper (FSM, forced tools, output validation, bounded retry) is mixed — one cell better, two worse. Residual variance is the unconstrained free-text plan. Schema-validate the plan before any tool: 3/4 cells hit reproducibility 1.000 at N=100 and 100% task success. Token cost drops; latency splits by model. Zero-Shot Self-Orchestration (2608.26480): a manager-worker scaffold over a shared filesystem, no training, no per-bench tune. Real but conditional — Qwen3.8-27B +23.4, Kimi-K3 +30.4 with reasoning off; Qwen3.6-35B null or negative. Manager triples tokens; GPT-5.6-Terra + manager nearly matches Fable 5 single-call accuracy at one-fifth the dollar cost. The gain is context management and decomposition, not a society of minds.

X useful: Sid Jain — prompt tells, context decides what is in view, harness is the world the model acts in. Skip: recycled 9-page "Agent = Model + Harness" PDFs (Annatar 29 Aug); HarnessRouter ads; coin / wellness "agentic"; MCP as a hashtag on unrelated posts. Heat 1.

Publish: 6h lab — tag every context block `system | user | tool | memory` and refuse promotion. Compaction may shorten a tool block; it may not rewrite its role. Run one poisoned tool return twice: once with promotion allowed, once forbidden. Report whether the second action follows the tool text.
