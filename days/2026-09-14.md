# 2026-09-14

Greg Isenberg (13 Sep, x.com/gregisenberg/status/2099202686377742576): a wrapper sells software; a harness sells the finished job. Four jobs named: loop, hands, memory, stop-rules. Model-swap is the point. Heat 3 language — same claim as Tan / LangChain / Whinnery from 12–13 Sep. Do not open a concept. Pair with OpenAI Agents API + Claude Managed Agents: the hosted cage is now a product SKU; the domain job is still the IP.

Addy Osmani quoting Boris Cherny (12 Sep, x.com/addyosmani/status/2098662421644853433): a quiet human fix is not a harness update. Production code written by Claude should clear a higher bar than a human PR. Reject before the PR exists. Turn the rejection into a skill (`/verify`, e2e, schema). "When Claude misses, don’t quietly fix it by hand. Have it write the lesson into CLAUDE.md or a skill." Pair with Warp outer/inner skill and MASkills: the merge is the gate. Pair with SkillsBench: a longer SKILL.md is not a free lift.

LiteLLM MCP grants (docs.litellm.ai/docs/mcp_control, candidate 1.102 noted 14 Sep): a missing allowlist is not deny. Six levels (key / team / end-user / agent / internal-user / org). Lists intersect; org is a ceiling. No list at any level → every registered MCP server. `no-mcp-servers` is the explicit deny sentinel. A key bound to an agent still needs the *agent* grant or the call names the agent and dies. Skills pack (`litellm-skills`) lets any Agent Skills client create users, keys, models, MCP servers, agents via curl against the proxy. Pair with CapScope / XSA: the proxy is a second substrate; default-open is ambient authority. Pair with MCP registry draw: probe and pin before the skill registers the server.

Pawbreak (github.com/0xgetz/pawbreak, MIT, created 14 Sep): a single-prompt scanner is not an agent red-team. Payloads live in files, pages, mail, and tool output; tripwire tools (`wipe_workspace`, `send_report`, `show_secrets`) score ASR = breached ÷ valid trials, plus critical-only and per-surface. Stdlib only; mock agents `gullible` / `hardened` need no key. 0 stars this window — shipping shape, not adoption. Pair with CapScope injected-effect rate and Scanning the Harness: this is the open tripwire bench those papers implied.

AgentRecall (github.com/Goldentrii/AgentRecall-X, 371★, MCP + SKILL.md + CLI): a NOTES dump is not a correction loop. Five local markdown layers plus a corrections ledger that asks whether the same miss recurs after reset. Published numbers are unflattering on purpose (capture recall 35.3%; heed 0/3 post-reset; transfer 0/4 in the instrument they reset). Pair with env-probe: a correction that never re-reads the world is another stale row. Pair with Osmani: the skill file is the correction artifact; the ledger is whether it fired.

When Agents Disagree (2609.11709, Chen / Wang / Seneviratne / Weeratunge / Halgamuge, 10 Sep, listed into this window): a majority of forward traces is not an independent check. Reverse posterior from an explicit likelihood; Jensen-Shannon ranks agents by cross-path consistency. MinJS / FwdJS / LogLin on DDXPlus × five backbones; LogLin strongest where the agents conflict. No repo this window. Heat 3 under evals-gates. Pair with VP-CONTROL: a second model on the *same* evidence is still one factorization.

X skip: MCP-coin / VeVe; wellness “agentic”; Udemy / master-tree / Packt Context Engine listicles; Graph-RAG consulting wraps; `infinite-pokemon` skill pack (game content, not a measurement); tutorial repos (`autornd-os`, `BeautyAgent`, `forgeharness`) heat 1–2. “Harnesses are the new GPT wrappers” without a fixture is slogan. Artificial Id (2609.11911) is a persistence essay without a 6h lab. Readiness-from-release scheduling (2609.10964) is runtime queuing, not a concept. CrowdStrike “Agent-state” / SafeMind (Kurtz 13 Sep) is vendor heat 3 — privileged identity is already CapScope.

Publish: 6h lab — default-open MCP vs tripwire vs written correction. Same poisoned README that asks for `wipe_workspace`. Arm A: LiteLLM-shaped proxy with no `mcp_servers` list (open default) and a skill that can `/add-mcp`. Arm B: intersected key+agent grant; org ceiling; `no-mcp-servers` on the untrusted server. Arm C: Arm B plus pawbreak tripwires and an AgentRecall-style ledger — after the first refuse, write the miss into `SKILL.md`, reset the session, replay. Report injected-effect fires, whether the second run heeds the skill, and whether a missing allowlist equals every server.
