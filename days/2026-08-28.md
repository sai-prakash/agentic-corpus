# 2026-08-28

JIT-Agent (2608.25593 + github.com/bingreeky/JIT): harness intelligence is trainable and orthogonal to model scaling. Four-module protocol (memory, planning, action, capability). Frozen generator, evolving archive. DeepSeek-V4-Flash + JIT beats GPT-5.6 on DeepSearchQA. This is StarHarness taken off the human outer loop.

Anthropic Model Hardware Standard (27 Aug research preview): MCP for devices. Driver carries read/write primitives and safety tags (arm weight, temperature limits). Three control paths: MCP, CLI, code file that keeps running after the model stops. Safety lives in the driver, not the prompt. Partners: Janelia, Genentech, QuEra, HF LeRobot, Raspberry Pi. Not open source yet.

CaSKG (2608.25500) vs Graph-of-Skills: calibrate skill *edges* with counterfactual probes before retrieval. Do not dump the library. SelfGraphRAG (2608.25123) is the same move on document graphs — structure supervises the retriever when labels are missing.

X useful: 0xnoonez splits harness (allowed) / context (in view) / memory (survives). SatyKrish: proactive agents need permissions and evals *out of the prompt*, as graph nodes. Vtrivedy: harness first so GRPO has a 0.2–0.6 pass@k to amplify. 2abstract4me: Red Tape eval for week-scale sessions.

Skip: Karpathy/Krieger "95% graph engineering" engagement wraps; Google 9-page PDF slop; HarnessRouter ads; coin/wellness "agentic". Heat 1.

Publish: 6h lab — freeze the model, implement the four-module protocol as files, swap one module, show the eval delta. Thread MHS only after the lab names where the kill-switch lives.
